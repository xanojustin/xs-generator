# FEEDBACK.md - Xano MCP/XanoScript Feedback

## [2025-02-28 12:05 PST] - No Issues Encountered

**What I was trying to do:** Create a complete XanoScript exercise (run job + function) for the Coin Change II problem

**What the issue was:** No issues encountered - both files passed validation on the first attempt

**Why it was an issue:** N/A - successful implementation

**Potential solution (if known):** The documentation from `xanoscript_docs` was clear and comprehensive. The key insights that helped:
- Understanding the run.job structure with `main = { name: "...", input: { ... } }`
- Function structure with `input { }`, `stack { }`, and `response = $result`
- Using proper type names (`int`, `int[]`, `text`)
- Understanding the `|get:` filter for array/object access with string keys
- Proper loop structures (foreach, for, while)

---
